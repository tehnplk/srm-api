# รายการโมดูลในระบบ SRM API

## ภาพรวมระบบ

SRM API เป็นแอปพลิเคชัน PyQt6 สำหรับจัดการข้อมูลประชากรและผู้รับบริการในระบบ HIS (Hospital Information System)

---

## 📋 โมดูลหลัก (Main Modules)

### 1. Main Module
- **ไฟล์**: `Main.py`, `Main_ui.py`
- **ฟังก์ชันการทำงาน**:
  - หน้าต่างหลักของโปรแกรม (QMainWindow)
  - จัดการเมนูและ Toolbar
  - ควบคุมการแสดงผล MDI (Multiple Document Interface)
  - เชื่อมต่อกับโมดูลอื่นๆ ผ่านเมนู
  - จัดการการจัดหน้าต่าง (Tile, Cascade, Close All)

---

## 🔐 โมดูลเข้าสู่ระบบ (Authentication Module)

### 2. Login Module
- **ไฟล์**: `Login.py`, `Login_ui.py`
- **ฟังก์ชันการทำงาน**:
  - หน้าจอเข้าสู่ระบบ (Dummy implementation)
  - ตรวจสอบชื่อผู้ใช้และรหัสผ่าน
  - รองรับการจดจำผู้ใช้
  - จำกัดจำนวนครั้งในการพยายามเข้าสู่ระบบ (3 ครั้ง)
  - แสดงข้อความผิดพลาดเมื่อเข้าสู่ระบบไม่สำเร็จ
  - บัญชีทดสอบ: admin/1234, user/password, test/test, demo/demo
  - ใช้ UI สมัยใหม่ด้วย Light theme

---

## 👥 โมดูลผู้ป่วย (Patient Modules)

### 3. Patient Module
- **ไฟล์**: `Patient.py`, `Patient_ui.py`
- **ฟังก์ชันการทำงาน**:
  - จัดการข้อมูลผู้ป่วยทั้งหมด
  - ค้นหาและแสดงรายชื่อผู้ป่วย
  - ตรวจสอบสิทธิการรักษา
  - แสดงข้อมูลสิทธิการรักษาในรูปแบบตาราง
  - ใช้ QThread สำหรับการตรวจสอบสิทธิแบบ asynchronous

### 4. PatientToday Module
- **ไฟล์**: `PatientToday.py`, `PatientToday_ui.py`
- **ฟังก์ชันการทำงาน**:
  - จัดการข้อมูลผู้รับบริการในวันปัจจุบัน
  - แสดงรายชื่อผู้ป่วยที่มาใช้บริการวันนี้
  - ตรวจสอบสิทธิผู้ป่วยวันนี้
  - กรองข้อมูลตามวันที่
  - ใช้ QThread สำหรับการประมวลผลข้อมูล

---

## 🔍 โมดูลตรวจสอบ (Validation Modules)

### 5. PersonalCheck Module
- **ไฟล์**: `PersonalCheck.py`, `PersonalCheck_ui.py`
- **ฟังก์ชันการทำงาน**:
  - ตรวจสอบสิทธิการรักษาแบบรายบุคคล
  - กรอกข้อมูล CID หรือ HN เพื่อตรวจสอบ
  - แสดงผลลัพธ์การตรวจสอบสิทธิ
  - แสดงข้อมูลสิทธิการรักษาที่ตรวจพบ
  - สามารถตรวจสอบได้ทั้งแบบ Manual และผ่าน Smart Card

### 6. F43ZipCheck Module
- **ไฟล์**: `F43ZipCheck.py`, `F43ZipCheck_ui.py`
- **ฟังก์ชันการทำงาน**:
  - ตรวจสอบไฟล์ ZIP ที่มี 43 แฟ้มข้อมูล
  - แตกไฟล์ ZIP และตรวจสอบความสมบูรณ์ของแฟ้ม
  - แสดงรายการแฟ้มที่พบและที่หายไป
  - ตรวจสอบรูปแบบและโครงสร้างของแฟ้ม
  - สร้างรายงานการตรวจสอบ

### 7. Export16Files Module
- **ไฟล์**: `Export16Files.py`, `Export16Files_ui.py`
- **ฟังก์ชันการทำงาน**:
  - ส่งออกข้อมูล 16 แฟ้มหลักของระบบ
  - เลือกช่วงวันที่สำหรับการส่งออก
  - รองรับหลายรูปแบบ (CSV, Excel, Text)
  - สามารถเลือกส่งออกเฉพาะแฟ้มที่ต้องการ
  - ใช้ QThread ป้องกัน UI freeze ระหว่างส่งออก
  - แสดง Progress และ Log การทำงานแบบ real-time

---

## ⚙️ โมดูลจัดการระบบ (System Management)

### 8. Setting Module
- **ไฟล์**: `Setting.py`, `Setting_ui.py`
- **ฟังก์ชันการทำงาน**:
  - ตั้งค่าการเชื่อมต่อ API
  - กำหนดค่าระบบต่างๆ
  - จัดการการตั้งค่าผู้ใช้
  - บันทึกและโหลดค่า config
  - ตั้งค่าพาธและที่เก็บข้อมูล

### 9. Update Module
- **ไฟล์**: `Update.py`, `Update_ui.py`
- **ฟังก์ชันการทำงาน**:
  - ตรวจสอบเวอร์ชันโปรแกรม
  - ดาวน์โหลดและติดตั้งอัปเดต
  - แสดงข้อมูลเวอร์ชันปัจจุบันและใหม่
  - ใช้ QThread สำหรับการดาวน์โหลดแบบ background
  - จัดการการอัปเดตแบบอัตโนมัติ

---

## 💾 โมดูลสำรองข้อมูล (Backup & Restore)

### 10. Backup Module
- **ไฟล์**: `Backup.py`, `Backup_ui.py`
- **ฟังก์ชันการทำงาน**:
  - สำรองข้อมูลฐานข้อมูล HIS
  - เลือกประเภทข้อมูลที่จะสำรอง
  - กำหนดตำแหน่งที่เก็บไฟล์ backup
  - บีบอัดข้อมูลเพื่อประหยัดพื้นที่
  - สร้างรายงานการสำรองข้อมูล

### 11. Restore Module
- **ไฟล์**: `Restore.py`, `Restore_ui.py`
- **ฟังก์ชันการทำงาน**:
  - กู้คืนข้อมูลจากไฟล์ backup
  - เลือกไฟล์ backup ที่จะกู้คืน
  - ตรวจสอบความสมบูรณ์ของไฟล์
  - แสดงข้อมูลที่จะกู้คืนก่อนดำเนินการ
  - ใช้ QThread สำหรับการกู้คืนข้อมูล

---

## 🏗️ โมดูลโครงสร้าง (Scaffolding Modules)

### 12. Scaffolding Modules
- **ไฟล์**: `scaffolding/Main.py`, `scaffolding/Main_ui.py`, `scaffolding/Child.py`, `scaffolding/Child_ui.py`
- **ฟังก์ชันการทำงาน**:
  - เป็นตัวอย่างโครงสร้างการสร้างโมดูลใหม่
  - แสดงวิธีการแยก UI และ Logic files
  - ใช้เป็น template สำหรับพัฒนาโมดูลเพิ่มเติม

### 13. WatermarkMdiArea Module
- **ไฟล์**: `scaffolding/WatermarkMdiArea.py`
- **ฟังก์ชันการทำงาน**:
  - สร้างพื้นหลัง MDI Area พร้อม watermark
  - จัดการการแสดงผลหน้าต่างย่อย
  - ปรับแต่งการแสดงผลของ MDI

---

## 🔧 โมดูลประกอบ (Supporting Files)

### 14. QtSmartCard Module
- **ไฟล์**: `QtSmartCard.py`
- **ฟังก์ชันการทำงาน**:
  - จัดการการอ่านข้อมูลจาก Smart Card
  - อ่านข้อมูลบัตรประชาชน
  - ส่งข้อมูล CID ให้กับโมดูลอื่นๆ
  - ใช้ QThread สำหรับการ monitor Smart Card
  - จัดการการเชื่อมต่อกับ card reader

### 15. Version Module
- **ไฟล์**: `Version.py`
- **ฟังก์ชันการทำงาน**:
  - เก็บข้อมูลเวอร์ชันของโปรแกรม
  - แสดง version name, release, และ code

### 16. Call Module
- **ไฟล์**: `call.py`
- **ฟังก์ชันการทำงาน**:
  - ฟังก์ชันสำหรับการเรียก API
  - จัดการ HTTP requests

### 17. Refresh Module
- **ไฟล์**: `refresh.py`
- **ฟังก์ชันการทำงาน**:
  - ฟังก์ชันสำหรับการ refresh ข้อมูล
  - อัปเดตข้อมูลแบบ real-time

---

## 📋 การเข้าถึงโมดูล

### ทางเมนู (Menu Access):
- **File Menu**:
  - `🔐 เข้าสู่ระบบ` → Login Module
  - `📋 รายชื่อทั้งหมด` → Patient Module
  - `📅 ผู้รับบริการวันนี้` → PatientToday Module
  - `👤 ตรวจสอบสิทธิ` → PersonalCheck Module
  - `📦 ตรวจสอบ43แฟ้ม (ZIP)` → F43ZipCheck Module
  - `📤 ส่งออก 16 แฟ้ม` → Export16Files Module
  - `⚙️ ตั้งค่า` → Setting Module
  - `🗄️ Backup HIS` → Backup Module
  - `🧩 Restore HIS` → Restore Module

- **Help Menu**:
  - `🔄 ตรวจสอบเวอร์ชัน` → Update Module
  - `ℹ️ เกี่ยวกับ` → About Dialog

### ทาง Toolbar (Toolbar Access):
- ปุ่มลัดสำหรับฟังก์ชันหลักที่ใช้บ่อย
- สามารถคลิกเพื่อเปิดโมดูลที่ต้องการได้ทันที

---

## 🏗️ โครงสร้างการพัฒนา

### มาตรฐานการสร้างโมดูล:
1. **แยกไฟล์**: UI file (`_ui.py`) และ Logic file (`.py`)
2. **ใช้ QThread**: สำหรับงานที่ใช้เวลานาน
3. **Modern UI**: ออกแบบใน Light theme
4. **Error Handling**: มีการจัดการข้อผิดพลาดที่เหมาะสม
5. **Menu Integration**: สามารถเปิดจากเมนูและ toolbar

### การเพิ่มโมดูลใหม่:
1. สร้าง UI file และ Logic file
2. เพิ่ม action ใน `Main_ui.py`
3. เชื่อมต่อ signal ใน `Main.py`
4. เพิ่ม show method ใน `Main.py`

---

## 📊 สรุป

ระบบมีทั้งหมด **17 โมดูลหลัก** แบ่งเป็น:
- **โมดูลหลัก**: 1 โมดูล
- **โมดูลเข้าสู่ระบบ**: 1 โมดูล
- **โมดูลผู้ป่วย**: 2 โมดูล
- **โมดูลตรวจสอบ**: 3 โมดูล  
- **โมดูลจัดการระบบ**: 2 โมดูล
- **โมดูลสำรองข้อมูล**: 2 โมดูล
- **โมดูลโครงสร้าง**: 2 โมดูล
- **โมดูลประกอบ**: 4 โมดูล

ทุกโมดูลถูกออกแบบมาให้สามารถทำงานแยกกันได้ แต่สามารถเชื่อมต่อกับโมดูลอื่นๆ ผ่าน Main Module ได้อย่างมีประสิทธิภาพ
